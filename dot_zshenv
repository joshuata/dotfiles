# export PATH="$HOME/.local/bin":"$PATH"
export DEFAULT_USER='joshuata'
export ZSH_CUSTOM="${HOME}/.zsh"

path=(
  $HOME/.local/bin
  /usr/local/{bin,sbin}
  $path
)
export PATH

[[ ! -f "$HOME/.cargo/env" ]] || source "$HOME/.cargo/env"
